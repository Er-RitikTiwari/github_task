name: first-app

on:
  push:
    branches:
      - main

jobs:
  build:
    uses: Er-RitikTiwari/github_task/.github/workflows/reusable.yml@main
    with:
      node-version: 20
      app-name: "app1"
